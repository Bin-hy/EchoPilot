# EchoPilot Checklist

> 每一项通过运行代码或观察行为验证，聚焦系统行为，不依赖具体代码结构。

## 实现完整性
- [ ] sidecar 服务可启动且健康检查通过（验证：启动后请求 /health 返回 ok）
- [ ] 双通道采集可用（验证：播放系统音频 + 对麦克风说话，两路转写分别标注
      "面试官"/"我"，串轨率 < 10%）— AC1
- [ ] 通道自检可见（验证：关闭系统音频权限后开始监听，界面明确指出哪个通道
      异常及修复方式，不静默转写为空）— AC2, F3
- [ ] 档案卡片化生效（验证：保存含"Kafka 项目"的简历档案后，相关问题能检索到
      对应卡片）— F8
- [ ] 档案隔离生效（验证：建 2 份不同档案切换后，同一问题的参考回答各自锚定
      对应简历素材，内容明显不同）— AC10

## 问题检测（回放 20 段标注面试样本）
- [ ] 完整问题召回率 ≥ 80%（验证：跑样本统计触发数/标注数）— AC4
- [ ] 附和零触发（验证：样本中"嗯/对/好的"段落无任何生成触发）— AC4
- [ ] 祈使句可触发（验证："介绍一下你自己""聊聊你的项目"样本均触发生成）— AC5
- [ ] 手动兜底可用（验证：自动检测失效时按快捷键 1 秒内触发生成；同一问题
      可一键重新生成且旧回答被替换）— AC6, F5

## 回答生成
- [ ] 无素材不编造（验证：用档案外的"跨境电商项目"提问，回答明确提示无对应
      素材并仅给思路方向，无编造的项目名/公司名/数字）— AC7, F6
- [ ] 两级展示时序正确（验证：提纲先于完整稿出现；完整稿流式追加不阻塞提纲）— F7
- [ ] 档位字数合规（验证：三档各生成 3 次，字数落在 60–90 / 150–220 /
      300–400 区间）— AC8
- [ ] 幻觉实体标红（验证：构造一次含档案外公司名的生成，该实体在浮窗标红）— AC9

## 端到端延迟（N1/N2，生死线）
- [ ] 20 次取样：面试官说完 → 提纲出现 P50 ≤ 3s、P90 ≤ 5s
      （验证：latency_ms 埋点统计报告）— AC8
- [ ] 完整稿在提纲出现后 5s 内流完（验证：同批取样统计）— N2

## 集成
- [ ] 启动 App 自动拉起 sidecar，杀掉 sidecar 后 5 秒内自动重启且界面
      提示状态（验证：观察进程与界面横幅）— N8
- [ ] 全局快捷键在焦点位于其他应用时生效（验证：焦点在 Zoom 窗口按
      开始/停止/重生成/切档位均生效）— N7
- [ ] 浮窗置顶于会议窗口之上，透明度/位置调整即时生效（验证：开 Zoom
      实测）— AC11, F9
- [ ] 断网或 Key 失效 2 秒内浮窗显示明确异常状态（验证：拔网线、
      配置错误 Key 各测一次）— AC14
- [ ] 切换 ASR/LLM 服务商（2 家 OpenAI 兼容）后全链路可用（验证：改
      base_url + key 重跑回放链路）— AC15, N10

## 数据与隐私
- [ ] 完整面试记录可回看（验证：结束会话后历史页可见双通道转写含说话人/
      时间戳 + 每个问答对）— AC12
- [ ] 删除无残留（验证：删除会话后历史页消失，本地数据库查询无该会话
      任何记录）— AC12
- [ ] Key 不明文落盘（验证：配置 Key 后全盘搜索应用数据目录无该字符串）— N6
- [ ] 无意外网络外联（验证：运行期间网络监控仅见用户配置的 ASR/LLM
      服务商域名，无其他外联）— N5

## 稳定性
- [ ] 连续监听 2 小时（回放长音频）无断流、无崩溃，内存增长 < 20%
      （验证：观察转写连续性与进程内存曲线）— AC13, N3

## 端到端场景
- [ ] 完整面试流程：新建档案（简历+JD）→ 通道自检 → 开始监听 → 回放
      10 分钟模拟面试 → 自动触发 ≥ 3 个问题并展示提纲+完整稿 →
      手动重生成 1 次 → 结束面试 → 历史页回看全部记录 → 删除记录
      （验证：全程无人值守跑通，每步结果可见）
