# EchoPilot — 实时面试参考助手 Spec

## 背景
远程视频面试（Zoom/腾讯会议/飞书）已成为求职主流场景。面试中候选人常因紧张、
大脑空白或语言组织困难而表现失常。现有方案（提前背稿、通用 AI 聊天窗口）要么
无法应对随机提问，要么需要手动输入问题、延迟过高，无法在面试现场使用。

EchoPilot 通过捕获电脑音频、实时识别面试官提问，结合候选人简历与目标岗位 JD，
在数秒内生成个性化参考回答，以提纲形式呈现在置顶提示窗口中，帮助候选人现场
组织语言。

## 目标
- G1: 面试官说完一个问题后 3 秒内（P50），提示窗口出现可用的回答提纲
- G2: 参考回答严格锚定候选人真实简历素材，不编造经历、数字、公司名
- G3: 全链路无需手动输入：音频采集 → 识别 → 生成 → 展示全自动，手动快捷键仅作兜底
- G4: 完整保存面试转写与问答记录，供面试后复盘
- G5: 单平台（macOS）先行验证，架构上为后续跨平台预留空间

## 功能需求

### 音频与识别
- F1: 双通道音频采集——同时捕获系统音频（面试官声音）与麦克风（用户本人声音），
      两路独立成轨，以通道归属作为说话人区分的主依据
- F2: 实时流式转写——对两路音频分别进行流式语音识别，逐句输出带说话人标签
      （面试官/我）和时间戳的转写文本，滚动展示在转写区
- F3: 采集状态自检——开始监听前自动检测系统音频与麦克风通道是否正常，
      异常时给出明确原因与修复指引（如权限未授予），不允许静默失败

### 问题检测与回答生成
- F4: 自动问题检测——从面试官通道的连续转写流中判定「面试官说完了一个期望
      候选人回答的完整问题」并自动触发回答生成；判定需覆盖疑问句与祈使句
      （如"聊聊你们的分区策略"），过滤"嗯/对/好的"等附和无意义片段
- F5: 手动触发兜底——提供全局快捷键，用户可随时手动将当前转写内容作为问题
      触发生成，或在上次判定不准时一键重新生成
- F6: 个性化参考回答——基于候选人简历与目标岗位 JD 生成第一人称、口语化的
      参考回答；回答中的经历、数字、公司名、技术栈必须来自用户档案，
      档案中没有对应素材时明确告知"无对应素材"并仅给出思路方向，不得编造
- F7: 两级展示——提示窗口先展示 3–5 条要点提纲（≤3 秒出现），随后流式补齐
      完整参考回答；用户可切换回答长度档位（简洁/标准/详细）；
      生成内容与简历素材不一致的实体（公司名/数字/项目名）需标红警示

### 面试前准备与面试后复盘
- F8: 面试档案管理——创建/编辑多份面试档案，每份包含简历文本 + 目标岗位 JD；
      开始监听前选择本次使用的档案
- F9: 提示窗口——置顶半透明浮窗，位置/透明度/字号可调，展示当前识别到的
      问题与参考回答，不遮挡面试主窗口的关键区域
- F10: 面试记录存档——面试结束后保存完整双通道转写（含说话人与时间戳）及
       每场问答对（问题 + 生成的参考回答），本地存储，可回看、可删除

## 非功能需求

### 性能（实时性）
- N1: 端到端延迟——面试官说完最后一字到提纲出现在提示窗口：P50 ≤ 3s，P90 ≤ 5s
- N2: 完整参考回答流式输出不阻塞提纲展示，完整稿在提纲出现后 5s 内流完
- N3: 长时间运行稳定——连续监听 2 小时无内存泄漏、无音频断流、无转写中断

### 隐私与安全
- N4: 音频数据不做云端持久化——仅转写文本与档案内容发送至云端 ASR/LLM 服务，
      原始音频经流式 ASR 处理即弃，不落盘、不做云端持久化
- N5: 全部历史数据（转写、问答对、档案）仅存本地，应用无账号体系、
      不主动向自建服务器上报任何内容
- N6: API Key 由用户自行配置，本地安全存储（系统钥匙串），不明文落盘

### 可用性
- N7: 关键操作可单手完成——开始/停止监听、重新生成、切换档位均有全局快捷键，
      面试中无需将鼠标移出面试窗口
- N8: 异常可感知——音频断流、API 额度耗尽、网络中断、识别异常时，提示窗口
      在 2 秒内给出明确状态提示，不静默失效

### 兼容性
- N9: 支持 macOS 13+（系统音频采集的最低系统要求）
- N10: ASR 与 LLM 服务通过 OpenAI 兼容协议接入，用户可切换服务商与模型，
       不锁定单一供应商

## 不做的事

### 本期不做（留待后续版本）
- O1: 声纹注册与多面试官区分（V0.1 依赖双通道归属区分说话人，会议中多位
      面试官统一标记为"面试官"，不区分 A/B）
- O2: AI 复盘报告（回答质量点评、差异分析、知识点建议）——V0.1 仅保存原始记录
- O3: 练习模式（AI 扮演面试官）——本期专注实战辅助链路，练习模式下期交付；
      V0.1 链路测试改用回放真实面试录音/播客的方式
- O4: 提示窗口防截屏/防屏幕共享——技术可行性待验证，本期不承诺
- O5: 回答生成的预测性预取 / 推测生成（speculative generation）等进阶延迟优化

### 明确不做（产品边界）
- O6: 移动端与线下面试场景（隐藏耳机等硬件方案）——不做，定位桌面远程面试
- O7: 自研 ASR / 声纹 / 大模型——全部使用成熟云服务，不自训模型
- O8: 账号体系、云端同步、多设备——数据纯本地
- O9: 团队协作、题库共享、面试题库社区——与核心链路无关
- O10: Windows / Linux 支持——本期仅 macOS，架构预留跨平台能力
- O11: 实时翻译与跨语言面试辅助——本期假设面试语言为中文或英文单语种
- O12: 任何形式的"代面"协助（将面试实时转述给第三方作答）——不做

## 验收标准

### 音频与识别（F1–F3）
- AC1: 播放一段双人对话音频（系统音源）同时对着麦克风说话，转写区两路内容
       分别正确标注"面试官"/"我"，串轨率 < 10%
- AC2: 拔掉麦克风或关闭系统音频权限后开始监听，应用明确指出哪个通道异常及
       修复方式，而非无提示地转写为空
- AC3: 与 Zoom/腾讯会议真实通话时，系统音频通道完整捕获对方声音，无断流

### 问题检测与回答生成（F4–F7）
- AC4: 回放 20 段真实/模拟面试片段，完整问题的自动检测召回率 ≥ 80%，
       "嗯/好的"等附和零触发
- AC5: 祈使句式提问（"介绍一下你自己""聊聊你的项目"）能正确触发生成
- AC6: 自动检测失效时，手动快捷键 1 秒内触发生成；对同一问题可一键重新生成
- AC7: 提问涉及档案中不存在的经历时（如档案无"跨境电商项目"），回答明确
       提示无对应素材并仅给思路，不编造项目名、公司名或数字
- AC8: 提纲在面试官说完后 P50 ≤ 3s 出现（实测 20 次取样）；完整稿流式
       补齐；长度档位切换后回答字数落在对应区间（简洁 60–90 字 /
       标准 150–220 字 / 详细 300–400 字）
- AC9: 生成回答中若出现档案外的公司名/数字/项目名，该实体在提示窗口标红
       （人工构造一次模型"幻觉"输入验证标红逻辑生效）

### 档案、窗口与记录（F8–F10）
- AC10: 创建 2 份不同档案（不同简历 + JD），切换档案后同一问题的参考回答
        内容明显不同且各自锚定对应简历素材
- AC11: 提示窗口始终置顶于 Zoom/腾讯会议窗口之上，透明度与位置调整即时生效
- AC12: 结束一场完整面试后，本地可回看：双通道完整转写（含说话人/时间戳）
        + 每个问题及其参考回答；删除记录后本地无残留

### 非功能验收（N1–N10）
- AC13: 连续监听 2 小时（回放长音频），无断流、无崩溃，内存增长 < 20%
- AC14: 断网或 API Key 失效后 2 秒内提示窗口显示明确异常状态
- AC15: 切换 ASR/LLM 服务商配置（至少验证 2 家 OpenAI 兼容服务）后全链路可用
